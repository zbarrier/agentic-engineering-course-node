"use strict";Object.defineProperty(exports, "__esModule", {value: true}); function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; } function _nullishCoalesce(lhs, rhsFn) { if (lhs != null) { return lhs; } else { return rhsFn(); } } function _optionalChain(ops) { let lastAccessLHS = undefined; let value = ops[0]; let i = 1; while (i < ops.length) { const op = ops[i]; const fn = ops[i + 1]; i += 2; if ((op === 'optionalAccess' || op === 'optionalCall') && value == null) { return undefined; } if (op === 'access' || op === 'optionalAccess') { lastAccessLHS = value; value = fn(value); } else if (op === 'call' || op === 'optionalCall') { value = fn((...args) => value.call(lastAccessLHS, ...args)); lastAccessLHS = undefined; } } return value; } var _class;


















var _chunkWND32L6Pcjs = require('./chunk-WND32L6P.cjs');

// src/typing/command.ts
var command = (...args) => {
  const [type, data, metadata] = args;
  return metadata !== void 0 ? { type, data, metadata, kind: "Command" } : { type, data, kind: "Command" };
};

// src/typing/event.ts
var event = (...args) => {
  const [type, data, metadata] = args;
  return metadata !== void 0 ? { type, data, metadata, kind: "Event" } : { type, data, kind: "Event" };
};

// src/typing/message.ts
var message = (...args) => {
  const [kind, type, data, metadata] = args;
  return metadata !== void 0 ? { type, data, metadata, kind } : { type, data, kind };
};

// src/typing/workflow.ts
var reply = (message2) => {
  return {
    action: "Reply",
    message: message2
  };
};
var send = (message2) => {
  return {
    action: "Send",
    message: message2
  };
};
var publish = (message2) => {
  return {
    action: "Publish",
    message: message2
  };
};
var schedule = (message2, when) => {
  return {
    action: "Schedule",
    message: message2,
    when
  };
};
var complete = () => {
  return {
    action: "Complete"
  };
};
var ignore = (reason) => {
  return {
    action: "Ignore",
    reason
  };
};
var error = (reason) => {
  return {
    action: "Error",
    reason
  };
};
var accept = () => {
  return { action: "Accept" };
};

// src/typing/index.ts
var emmettPrefix = "emt";
var globalTag = "global";
var defaultTag = `${emmettPrefix}:default`;
var unknownTag = `${emmettPrefix}:unknown`;

// src/eventStore/afterCommit/afterEventStoreCommitHandler.ts
async function tryPublishMessagesAfterCommit(messages, options, context) {
  if (_optionalChain([options, 'optionalAccess', _2 => _2.onAfterCommit]) === void 0) return false;
  try {
    await _optionalChain([options, 'optionalAccess', _3 => _3.onAfterCommit, 'call', _4 => _4(messages, context)]);
    return true;
  } catch (error2) {
    console.error(`Error in on after commit hook`, error2);
    return false;
  }
}

// src/eventStore/afterCommit/forwardToMessageBus.ts
var forwardToMessageBus = (eventPublisher) => (async (messages) => {
  for (const message2 of messages) {
    await eventPublisher.publish(message2);
  }
});

// src/eventStore/events/index.ts
var GlobalStreamCaughtUpType = "__emt:GlobalStreamCaughtUp";
var isGlobalStreamCaughtUp = (event2) => event2.type === GlobalStreamCaughtUpType;
var caughtUpEventFrom = (position) => (event2) => event2.type === GlobalStreamCaughtUpType && _optionalChain([event2, 'access', _5 => _5.metadata, 'optionalAccess', _6 => _6.globalPosition]) >= position;
var globalStreamCaughtUp = (data) => event(GlobalStreamCaughtUpType, data, {
  globalPosition: data.globalPosition
});
var isSubscriptionEvent = (event2) => isGlobalStreamCaughtUp(event2);
var isNotInternalEvent = (event2) => !isGlobalStreamCaughtUp(event2);

// src/eventStore/eventStore.ts
var canCreateEventStoreSession = (eventStore) => "withSession" in eventStore;
var nulloSessionFactory = (eventStore) => ({
  withSession: (callback) => {
    const nulloSession = {
      eventStore,
      close: () => Promise.resolve()
    };
    return callback(nulloSession);
  }
});

// src/eventStore/expectedVersion.ts
var STREAM_EXISTS = "STREAM_EXISTS";
var STREAM_DOES_NOT_EXIST = "STREAM_DOES_NOT_EXIST";
var NO_CONCURRENCY_CHECK = "NO_CONCURRENCY_CHECK";
var matchesExpectedVersion = (current, expected, defaultVersion) => {
  if (expected === NO_CONCURRENCY_CHECK) return true;
  if (expected == STREAM_DOES_NOT_EXIST) return current === defaultVersion;
  if (expected == STREAM_EXISTS) return current !== defaultVersion;
  return current === expected;
};
var assertExpectedVersionMatchesCurrent = (current, expected, defaultVersion) => {
  expected ??= NO_CONCURRENCY_CHECK;
  if (!matchesExpectedVersion(current, expected, defaultVersion))
    throw new ExpectedVersionConflictError(current, expected);
};
var ExpectedVersionConflictError = class _ExpectedVersionConflictError extends _chunkWND32L6Pcjs.ConcurrencyError {
  constructor(current, expected) {
    super(_optionalChain([current, 'optionalAccess', _7 => _7.toString, 'call', _8 => _8()]), _optionalChain([expected, 'optionalAccess', _9 => _9.toString, 'call', _10 => _10()]));
    Object.setPrototypeOf(this, _ExpectedVersionConflictError.prototype);
  }
};
var isExpectedVersionConflictError = (error2) => error2 instanceof ExpectedVersionConflictError || _chunkWND32L6Pcjs.EmmettError.isInstanceOf(
  error2,
  ExpectedVersionConflictError.Codes.ConcurrencyError
);

// src/eventStore/inMemoryEventStore.ts
var _uuid = require('uuid');

// src/database/inMemoryDatabase.ts


// src/utils/collections/duplicates.ts
var hasDuplicates = (array, predicate) => {
  const mapped = array.map(predicate);
  const uniqueValues = new Set(mapped);
  return uniqueValues.size < mapped.length;
};
var getDuplicates = (array, predicate) => {
  const map = /* @__PURE__ */ new Map();
  for (let i = 0; i < array.length; i++) {
    const item = array[i];
    const key = predicate(item, i, array);
    if (!map.has(key)) {
      map.set(key, []);
    }
    map.get(key).push(item);
  }
  return Array.from(map.values()).filter((group) => group.length > 1).flat();
};

// src/utils/collections/merge.ts
var merge = (array, item, where, onExisting, onNotFound = () => void 0) => {
  let wasFound = false;
  const result = array.map((p) => {
    if (!where(p)) return p;
    wasFound = true;
    return onExisting(p);
  }).filter((p) => p !== void 0).map((p) => {
    if (!p) throw Error("That should not happen");
    return p;
  });
  if (!wasFound) {
    const result2 = onNotFound();
    if (result2 !== void 0) return [...array, item];
  }
  return result;
};

// src/utils/collections/index.ts
var arrayUtils = {
  merge,
  hasDuplicates,
  getDuplicates
};

// src/utils/deepEquals.ts
var isPrimitive = (value) => {
  const type = typeof value;
  return value === null || value === void 0 || type === "boolean" || type === "number" || type === "string" || type === "symbol" || type === "bigint";
};
var compareArrays = (left, right) => {
  if (left.length !== right.length) {
    return false;
  }
  for (let i = 0; i < left.length; i++) {
    const leftHas = i in left;
    const rightHas = i in right;
    if (leftHas !== rightHas) return false;
    if (leftHas && !deepEquals(left[i], right[i])) return false;
  }
  return true;
};
var compareDates = (left, right) => {
  return left.getTime() === right.getTime();
};
var compareRegExps = (left, right) => {
  return left.toString() === right.toString();
};
var compareErrors = (left, right) => {
  if (left.message !== right.message || left.name !== right.name) {
    return false;
  }
  const leftKeys = Object.keys(left);
  const rightKeys = Object.keys(right);
  if (leftKeys.length !== rightKeys.length) return false;
  const rightKeySet = new Set(rightKeys);
  for (const key of leftKeys) {
    if (!rightKeySet.has(key)) return false;
    if (!deepEquals(left[key], right[key])) return false;
  }
  return true;
};
var compareMaps = (left, right) => {
  if (left.size !== right.size) return false;
  for (const [key, value] of left) {
    if (isPrimitive(key)) {
      if (!right.has(key) || !deepEquals(value, right.get(key))) {
        return false;
      }
    } else {
      let found = false;
      for (const [rightKey, rightValue] of right) {
        if (deepEquals(key, rightKey) && deepEquals(value, rightValue)) {
          found = true;
          break;
        }
      }
      if (!found) return false;
    }
  }
  return true;
};
var compareSets = (left, right) => {
  if (left.size !== right.size) return false;
  for (const leftItem of left) {
    if (isPrimitive(leftItem)) {
      if (!right.has(leftItem)) return false;
    } else {
      let found = false;
      for (const rightItem of right) {
        if (deepEquals(leftItem, rightItem)) {
          found = true;
          break;
        }
      }
      if (!found) return false;
    }
  }
  return true;
};
var compareArrayBuffers = (left, right) => {
  if (left.byteLength !== right.byteLength) return false;
  const leftView = new Uint8Array(left);
  const rightView = new Uint8Array(right);
  for (let i = 0; i < leftView.length; i++) {
    if (leftView[i] !== rightView[i]) return false;
  }
  return true;
};
var compareTypedArrays = (left, right) => {
  if (left.constructor !== right.constructor) return false;
  if (left.byteLength !== right.byteLength) return false;
  const leftArray = new Uint8Array(
    left.buffer,
    left.byteOffset,
    left.byteLength
  );
  const rightArray = new Uint8Array(
    right.buffer,
    right.byteOffset,
    right.byteLength
  );
  for (let i = 0; i < leftArray.length; i++) {
    if (leftArray[i] !== rightArray[i]) return false;
  }
  return true;
};
var compareObjects = (left, right) => {
  const keys1 = Object.keys(left);
  const keys2 = Object.keys(right);
  if (keys1.length !== keys2.length) {
    return false;
  }
  for (const key of keys1) {
    if (left[key] instanceof Function && right[key] instanceof Function) {
      continue;
    }
    const isEqual = deepEquals(left[key], right[key]);
    if (!isEqual) {
      return false;
    }
  }
  return true;
};
var getType = (value) => {
  if (value === null) return "null";
  if (value === void 0) return "undefined";
  const primitiveType = typeof value;
  if (primitiveType !== "object") return primitiveType;
  if (Array.isArray(value)) return "array";
  if (value instanceof Boolean) return "boxed-boolean";
  if (value instanceof Number) return "boxed-number";
  if (value instanceof String) return "boxed-string";
  if (value instanceof Date) return "date";
  if (value instanceof RegExp) return "regexp";
  if (value instanceof Error) return "error";
  if (value instanceof Map) return "map";
  if (value instanceof Set) return "set";
  if (value instanceof ArrayBuffer) return "arraybuffer";
  if (value instanceof DataView) return "dataview";
  if (value instanceof WeakMap) return "weakmap";
  if (value instanceof WeakSet) return "weakset";
  if (ArrayBuffer.isView(value)) return "typedarray";
  return "object";
};
var deepEquals = (left, right) => {
  if (left === right) return true;
  if (isEquatable(left)) {
    return left.equals(right);
  }
  const leftType = getType(left);
  const rightType = getType(right);
  if (leftType !== rightType) return false;
  switch (leftType) {
    case "null":
    case "undefined":
    case "boolean":
    case "number":
    case "bigint":
    case "string":
    case "symbol":
    case "function":
      return left === right;
    case "array":
      return compareArrays(left, right);
    case "date":
      return compareDates(left, right);
    case "regexp":
      return compareRegExps(left, right);
    case "error":
      return compareErrors(left, right);
    case "map":
      return compareMaps(
        left,
        right
      );
    case "set":
      return compareSets(left, right);
    case "arraybuffer":
      return compareArrayBuffers(left, right);
    case "dataview":
    case "weakmap":
    case "weakset":
      return false;
    case "typedarray":
      return compareTypedArrays(
        left,
        right
      );
    case "boxed-boolean":
      return left.valueOf() === right.valueOf();
    case "boxed-number":
      return left.valueOf() === right.valueOf();
    case "boxed-string":
      return left.valueOf() === right.valueOf();
    case "object":
      return compareObjects(
        left,
        right
      );
    default:
      return false;
  }
};
var isEquatable = (left) => {
  return left !== null && left !== void 0 && typeof left === "object" && "equals" in left && typeof left["equals"] === "function";
};

// src/utils/iterators.ts
var sum = (iterator) => {
  let value, done, sum2 = 0;
  do {
    ({ value, done } = iterator.next());
    sum2 += value || 0;
  } while (!done);
  return sum2;
};

// src/taskProcessing/taskProcessor.ts
var TaskProcessor = (_class = class {
  constructor(options) {;_class.prototype.__init.call(this);_class.prototype.__init2.call(this);_class.prototype.__init3.call(this);_class.prototype.__init4.call(this);_class.prototype.__init5.call(this);_class.prototype.__init6.call(this);
    this.options = options;
  }
  __init() {this.queue = []}
  __init2() {this.isProcessing = false}
  __init3() {this.activeTasks = 0}
  __init4() {this.activeGroups = /* @__PURE__ */ new Set()}
  enqueue(task, options) {
    if (this.queue.length >= this.options.maxQueueSize) {
      return Promise.reject(
        new (0, _chunkWND32L6Pcjs.EmmettError)(
          "Too many pending connections. Please try again later."
        )
      );
    }
    return this.schedule(task, options);
  }
  waitForEndOfProcessing() {
    return this.schedule(({ ack }) => Promise.resolve(ack()));
  }
  schedule(task, options) {
    return promiseWithDeadline(
      (resolve, reject) => {
        const taskWithContext = () => {
          return new Promise((resolveTask, failTask) => {
            const taskPromise = task({
              ack: resolveTask
            });
            taskPromise.then(resolve).catch((err) => {
              failTask(err);
              reject(err);
            });
          });
        };
        this.queue.push({ task: taskWithContext, options });
        if (!this.isProcessing) {
          this.ensureProcessing();
        }
      },
      { deadline: this.options.maxTaskIdleTime }
    );
  }
  ensureProcessing() {
    if (this.isProcessing) return;
    this.isProcessing = true;
    this.processQueue();
  }
  processQueue() {
    try {
      while (this.activeTasks < this.options.maxActiveTasks && this.queue.length > 0) {
        const item = this.takeFirstAvailableItem();
        if (item === null) return;
        const groupId = _optionalChain([item, 'access', _15 => _15.options, 'optionalAccess', _16 => _16.taskGroupId]);
        if (groupId) {
          this.activeGroups.add(groupId);
        }
        this.activeTasks++;
        void this.executeItem(item);
      }
    } catch (error2) {
      console.error(error2);
      throw error2;
    } finally {
      this.isProcessing = false;
      if (this.hasItemsToProcess() && this.activeTasks < this.options.maxActiveTasks) {
        this.ensureProcessing();
      }
    }
  }
  async executeItem({ task, options }) {
    try {
      await task();
    } finally {
      this.activeTasks--;
      if (options && options.taskGroupId) {
        this.activeGroups.delete(options.taskGroupId);
      }
      this.ensureProcessing();
    }
  }
  __init5() {this.takeFirstAvailableItem = () => {
    const taskIndex = this.queue.findIndex(
      (item2) => !_optionalChain([item2, 'access', _17 => _17.options, 'optionalAccess', _18 => _18.taskGroupId]) || !this.activeGroups.has(item2.options.taskGroupId)
    );
    if (taskIndex === -1) {
      return null;
    }
    const [item] = this.queue.splice(taskIndex, 1);
    return _nullishCoalesce(item, () => ( null));
  }}
  __init6() {this.hasItemsToProcess = () => this.queue.findIndex(
    (item) => !_optionalChain([item, 'access', _19 => _19.options, 'optionalAccess', _20 => _20.taskGroupId]) || !this.activeGroups.has(item.options.taskGroupId)
  ) !== -1}
}, _class);
var DEFAULT_PROMISE_DEADLINE = 2147483647;
var promiseWithDeadline = (executor, options) => {
  return new Promise((resolve, reject) => {
    let taskStarted = false;
    const maxWaitingTime = options.deadline || DEFAULT_PROMISE_DEADLINE;
    let timeoutId = setTimeout(() => {
      if (!taskStarted) {
        reject(
          new Error("Task was not started within the maximum waiting time")
        );
      }
    }, maxWaitingTime);
    executor((value) => {
      taskStarted = true;
      if (timeoutId) {
        clearTimeout(timeoutId);
      }
      timeoutId = null;
      resolve(value);
    }, reject);
  });
};

// src/utils/locking/index.ts
var InProcessLock = () => {
  const taskProcessor = new TaskProcessor({
    maxActiveTasks: Number.MAX_VALUE,
    maxQueueSize: Number.MAX_VALUE
  });
  const locks = /* @__PURE__ */ new Map();
  return {
    async acquire({ lockId }) {
      await new Promise((resolve, reject) => {
        taskProcessor.enqueue(
          ({ ack }) => {
            locks.set(lockId, ack);
            resolve();
            return Promise.resolve();
          },
          { taskGroupId: lockId }
        ).catch(reject);
      });
    },
    async tryAcquire({ lockId }) {
      if (locks.has(lockId)) {
        return false;
      }
      await this.acquire({ lockId });
      return true;
    },
    release({ lockId }) {
      const ack = locks.get(lockId);
      if (ack === void 0) {
        return Promise.resolve(true);
      }
      locks.delete(lockId);
      ack();
      return Promise.resolve(true);
    },
    async withAcquire(handle, { lockId }) {
      return taskProcessor.enqueue(
        async ({ ack }) => {
          locks.set(lockId, ack);
          try {
            return await handle();
          } finally {
            locks.delete(lockId);
            ack();
          }
        },
        { taskGroupId: lockId }
      );
    }
  };
};

// src/utils/numbers/bigint.ts
var toNormalizedString = (value) => value.toString().padStart(19, "0");
var bigInt = {
  toNormalizedString
};

// src/utils/promises.ts
var delay = (ms) => {
  return new Promise((resolve) => setTimeout(resolve, ms));
};
var asyncAwaiter = () => {
  const result = {};
  (result.reset = () => {
    result.wait = new Promise((res, rej) => {
      result.resolve = res;
      result.reject = rej;
    });
  })();
  return result;
};

// src/utils/retry.ts
var _asyncretry = require('async-retry'); var _asyncretry2 = _interopRequireDefault(_asyncretry);

// src/serialization/json/JSONParser.ts
var ParseError = class extends Error {
  constructor(text) {
    super(`Cannot parse! ${text}`);
  }
};
var JSONParser = {
  stringify: (value, options) => {
    return JSON.stringify(
      _optionalChain([options, 'optionalAccess', _21 => _21.map]) ? options.map(value) : value,
      //TODO: Consider adding support to DateTime and adding specific format to mark that's a bigint
      // eslint-disable-next-line @typescript-eslint/no-unsafe-return
      (_, v) => typeof v === "bigint" ? v.toString() : v
    );
  },
  parse: (text, options) => {
    const parsed = JSON.parse(text, _optionalChain([options, 'optionalAccess', _22 => _22.reviver]));
    if (_optionalChain([options, 'optionalAccess', _23 => _23.typeCheck]) && !_optionalChain([options, 'optionalAccess', _24 => _24.typeCheck, 'call', _25 => _25(parsed)]))
      throw new ParseError(text);
    return _optionalChain([options, 'optionalAccess', _26 => _26.map]) ? options.map(parsed) : parsed;
  }
};

// src/utils/retry.ts
var NoRetries = { retries: 0 };
var asyncRetry = async (fn, opts) => {
  if (opts === void 0 || opts.retries === 0) return fn();
  return _asyncretry2.default.call(void 0, 
    async (bail) => {
      try {
        const result = await fn();
        if (_optionalChain([opts, 'optionalAccess', _27 => _27.shouldRetryResult]) && opts.shouldRetryResult(result)) {
          throw new (0, _chunkWND32L6Pcjs.EmmettError)(
            `Retrying because of result: ${JSONParser.stringify(result)}`
          );
        }
        return result;
      } catch (error2) {
        if (_optionalChain([opts, 'optionalAccess', _28 => _28.shouldRetryError]) && !opts.shouldRetryError(error2)) {
          bail(error2);
          return void 0;
        }
        throw error2;
      }
    },
    _nullishCoalesce(opts, () => ( { retries: 0 }))
  );
};

// src/utils/shutdown/gracefulShutdown.ts
var onShutdown = (handler) => {
  const signals = ["SIGTERM", "SIGINT"];
  if (typeof process !== "undefined" && typeof process.on === "function") {
    for (const signal of signals) {
      process.on(signal, handler);
    }
    return () => {
      for (const signal of signals) {
        process.off(signal, handler);
      }
    };
  }
  const deno = globalThis.Deno;
  if (deno && typeof deno.addSignalListener === "function") {
    for (const signal of signals) {
      deno.addSignalListener(signal, handler);
    }
    return () => {
      for (const signal of signals) {
        deno.removeSignalListener(signal, handler);
      }
    };
  }
  return () => {
  };
};

// src/utils/strings/hashText.ts
var textEncoder = new TextEncoder();
var hashText = async (text) => {
  const hashBuffer = await crypto.subtle.digest(
    "SHA-256",
    textEncoder.encode(text)
  );
  const view = new BigInt64Array(hashBuffer, 0, 1);
  return view[0];
};

// src/database/utils.ts
var isGeneralExpectedDocumentVersion = (version) => {
  return version === "DOCUMENT_DOES_NOT_EXIST" || version === "DOCUMENT_EXISTS" || version === "NO_CONCURRENCY_CHECK";
};
var expectedVersionValue = (version) => version === void 0 || isGeneralExpectedDocumentVersion(version) ? null : version;
var operationResult = (result, options) => {
  const operationResult2 = {
    ...result,
    acknowledged: true,
    successful: result.successful,
    assertSuccessful: (errorMessage) => {
      const { successful } = result;
      const { operationName, collectionName } = options;
      if (!successful)
        throw new (0, _chunkWND32L6Pcjs.ConcurrencyInMemoryDatabaseError)(
          _nullishCoalesce(errorMessage, () => ( `${operationName} on ${collectionName} failed. Expected document state does not match current one! Result: ${JSONParser.stringify(result)}!`))
        );
    }
  };
  if (_optionalChain([options, 'access', _29 => _29.errors, 'optionalAccess', _30 => _30.throwOnOperationFailures]))
    operationResult2.assertSuccessful();
  return operationResult2;
};

// src/database/inMemoryDatabase.ts
var getInMemoryDatabase = () => {
  const storage = /* @__PURE__ */ new Map();
  return {
    collection: (collectionName, collectionOptions = {}) => {
      const ensureCollectionCreated = () => {
        if (!storage.has(collectionName)) storage.set(collectionName, []);
      };
      const errors = collectionOptions.errors;
      const collection = {
        collectionName,
        insertOne: async (document) => {
          ensureCollectionCreated();
          const _id = _nullishCoalesce(document._id, () => ( _uuid.v7.call(void 0, )));
          const _version = _nullishCoalesce(document._version, () => ( 1n));
          const existing = await collection.findOne((c) => c._id === _id);
          if (existing) {
            return operationResult(
              {
                successful: false,
                insertedId: null,
                nextExpectedVersion: _version
              },
              { operationName: "insertOne", collectionName, errors }
            );
          }
          const documentsInCollection = storage.get(collectionName);
          const newDocument = { ...document, _id, _version };
          const newCollection = [...documentsInCollection, newDocument];
          storage.set(collectionName, newCollection);
          return operationResult(
            {
              successful: true,
              insertedId: _id,
              nextExpectedVersion: _version
            },
            { operationName: "insertOne", collectionName, errors }
          );
        },
        findOne: (predicate) => {
          ensureCollectionCreated();
          const documentsInCollection = storage.get(collectionName);
          const filteredDocuments = predicate ? _optionalChain([documentsInCollection, 'optionalAccess', _31 => _31.filter, 'call', _32 => _32((doc) => predicate(doc))]) : documentsInCollection;
          const firstOne = _nullishCoalesce(_optionalChain([filteredDocuments, 'optionalAccess', _33 => _33[0]]), () => ( null));
          return Promise.resolve(firstOne);
        },
        find: (predicate) => {
          ensureCollectionCreated();
          const documentsInCollection = storage.get(collectionName);
          const filteredDocuments = predicate ? _optionalChain([documentsInCollection, 'optionalAccess', _34 => _34.filter, 'call', _35 => _35((doc) => predicate(doc))]) : documentsInCollection;
          return Promise.resolve(filteredDocuments);
        },
        deleteOne: (predicate) => {
          ensureCollectionCreated();
          const documentsInCollection = storage.get(collectionName);
          if (predicate) {
            const foundIndex = documentsInCollection.findIndex(
              (doc) => predicate(doc)
            );
            if (foundIndex === -1) {
              return Promise.resolve(
                operationResult(
                  {
                    successful: false,
                    matchedCount: 0,
                    deletedCount: 0
                  },
                  { operationName: "deleteOne", collectionName, errors }
                )
              );
            } else {
              const newCollection2 = documentsInCollection.toSpliced(
                foundIndex,
                1
              );
              storage.set(collectionName, newCollection2);
              return Promise.resolve(
                operationResult(
                  {
                    successful: true,
                    matchedCount: 1,
                    deletedCount: 1
                  },
                  { operationName: "deleteOne", collectionName, errors }
                )
              );
            }
          }
          const newCollection = documentsInCollection.slice(1);
          storage.set(collectionName, newCollection);
          return Promise.resolve(
            operationResult(
              {
                successful: true,
                matchedCount: 1,
                deletedCount: 1
              },
              { operationName: "deleteOne", collectionName, errors }
            )
          );
        },
        replaceOne: (predicate, document, options) => {
          ensureCollectionCreated();
          const documentsInCollection = storage.get(collectionName);
          const firstIndex = documentsInCollection.findIndex(
            (doc) => predicate(doc)
          );
          if (firstIndex === void 0 || firstIndex === -1) {
            return Promise.resolve(
              operationResult(
                {
                  successful: false,
                  matchedCount: 0,
                  modifiedCount: 0,
                  nextExpectedVersion: 0n
                },
                { operationName: "replaceOne", collectionName, errors }
              )
            );
          }
          const existing = documentsInCollection[firstIndex];
          if (typeof _optionalChain([options, 'optionalAccess', _36 => _36.expectedVersion]) === "bigint" && existing._version !== options.expectedVersion) {
            return Promise.resolve(
              operationResult(
                {
                  successful: false,
                  matchedCount: 1,
                  modifiedCount: 0,
                  nextExpectedVersion: existing._version
                },
                { operationName: "replaceOne", collectionName, errors }
              )
            );
          }
          const newVersion = existing._version + 1n;
          const newCollection = documentsInCollection.with(firstIndex, {
            _id: existing._id,
            ...document,
            _version: newVersion
          });
          storage.set(collectionName, newCollection);
          return Promise.resolve(
            operationResult(
              {
                successful: true,
                modifiedCount: 1,
                matchedCount: firstIndex,
                nextExpectedVersion: newVersion
              },
              { operationName: "replaceOne", collectionName, errors }
            )
          );
        },
        handle: async (id, handle, options) => {
          const { expectedVersion: version, ...operationOptions } = _nullishCoalesce(options, () => ( {}));
          ensureCollectionCreated();
          const existing = await collection.findOne(({ _id }) => _id === id);
          const expectedVersion = expectedVersionValue(version);
          if (existing == null && version === "DOCUMENT_EXISTS" || existing == null && expectedVersion != null || existing != null && version === "DOCUMENT_DOES_NOT_EXIST" || existing != null && expectedVersion !== null && existing._version !== expectedVersion) {
            return operationResult(
              {
                successful: false,
                document: existing
              },
              { operationName: "handle", collectionName, errors }
            );
          }
          const result = handle(existing !== null ? { ...existing } : null);
          if (deepEquals(existing, result))
            return operationResult(
              {
                successful: true,
                document: existing
              },
              { operationName: "handle", collectionName, errors }
            );
          if (!existing && result) {
            const newDoc = { ...result, _id: id };
            const insertResult = await collection.insertOne({
              ...newDoc,
              _id: id
            });
            return {
              ...insertResult,
              document: {
                ...newDoc,
                _version: insertResult.nextExpectedVersion
              }
            };
          }
          if (existing && !result) {
            const deleteResult = await collection.deleteOne(
              ({ _id }) => id === _id
            );
            return { ...deleteResult, document: null };
          }
          if (existing && result) {
            const replaceResult = await collection.replaceOne(
              ({ _id }) => id === _id,
              result,
              {
                ...operationOptions,
                expectedVersion: _nullishCoalesce(expectedVersion, () => ( "DOCUMENT_EXISTS"))
              }
            );
            return {
              ...replaceResult,
              document: {
                ...result,
                _version: replaceResult.nextExpectedVersion
              }
            };
          }
          return operationResult(
            {
              successful: true,
              document: existing
            },
            { operationName: "handle", collectionName, errors }
          );
        }
      };
      return collection;
    }
  };
};

// src/eventStore/projections/inMemory/inMemoryProjection.ts
var DATABASE_REQUIRED_ERROR_MESSAGE = "Database is required in context for InMemory projections";
var handleInMemoryProjections = async (options) => {
  const { projections: projections2, events, database, eventStore } = options;
  const eventTypes = events.map((e) => e.type);
  const relevantProjections = projections2.filter(
    (p) => p.canHandle.some((type) => eventTypes.includes(type))
  );
  for (const projection2 of relevantProjections) {
    await projection2.handle(events, {
      eventStore,
      database
    });
  }
};
var inMemoryProjection = ({
  truncate,
  handle,
  canHandle
}) => ({
  canHandle,
  handle: async (events, context) => {
    if (!context.database) {
      throw new Error(DATABASE_REQUIRED_ERROR_MESSAGE);
    }
    await handle(events, {
      ...context,
      database: context.database
    });
  },
  truncate: truncate ? (context) => {
    if (!context.database) {
      throw new Error(DATABASE_REQUIRED_ERROR_MESSAGE);
    }
    return truncate({
      ...context,
      database: context.database
    });
  } : void 0
});
var inMemoryMultiStreamProjection = (options) => {
  const { collectionName, getDocumentId, canHandle } = options;
  return inMemoryProjection({
    handle: async (events, { database }) => {
      const collection = database.collection(collectionName);
      for (const event2 of events) {
        await collection.handle(getDocumentId(event2), (document) => {
          if ("initialState" in options) {
            return options.evolve(_nullishCoalesce(document, () => ( options.initialState())), event2);
          } else {
            return options.evolve(document, event2);
          }
        });
      }
    },
    canHandle,
    truncate: async ({
      database
    }) => {
      const collection = database.collection(collectionName);
      const documents = await collection.find();
      for (const doc of documents) {
        if (doc && "_id" in doc) {
          const id = doc._id;
          await collection.deleteOne((d) => d._id === id);
        }
      }
    }
  });
};
var inMemorySingleStreamProjection = (options) => {
  return inMemoryMultiStreamProjection({
    ...options,
    getDocumentId: _nullishCoalesce(options.getDocumentId, () => ( ((event2) => event2.metadata.streamName)))
  });
};

// src/eventStore/projections/inMemory/inMemoryProjectionSpec.ts


// src/testing/assertions.ts
var AssertionError = class extends Error {
  constructor(message2) {
    super(message2);
  }
};
var isSubset = (superObj, subObj) => {
  const sup = superObj;
  const sub = subObj;
  assertOk(sup);
  assertOk(sub);
  return Object.keys(sub).every((ele) => {
    if (typeof sub[ele] == "object") {
      return isSubset(sup[ele], sub[ele]);
    }
    return sub[ele] === sup[ele];
  });
};
var assertFails = (message2) => {
  throw new AssertionError(_nullishCoalesce(message2, () => ( "That should not ever happened, right?")));
};
var assertThrowsAsync = async (fun, errorCheck) => {
  try {
    await fun();
  } catch (error2) {
    const typedError = error2;
    if (typedError instanceof AssertionError || !errorCheck) {
      assertFalse(
        typedError instanceof AssertionError,
        "Function didn't throw expected error"
      );
      return typedError;
    }
    assertTrue(
      errorCheck(typedError),
      `Error doesn't match the expected condition: ${JSONParser.stringify(error2)}`
    );
    return typedError;
  }
  throw new AssertionError("Function didn't throw expected error");
};
var assertThrows = (fun, errorCheck) => {
  try {
    fun();
  } catch (error2) {
    const typedError = error2;
    if (errorCheck) {
      assertTrue(
        errorCheck(typedError),
        `Error doesn't match the expected condition: ${JSONParser.stringify(error2)}`
      );
    } else if (typedError instanceof AssertionError) {
      assertFalse(
        typedError instanceof AssertionError,
        "Function didn't throw expected error"
      );
    }
    return typedError;
  }
  throw new AssertionError("Function didn't throw expected error");
};
var assertDoesNotThrow = (fun, errorCheck) => {
  try {
    fun();
    return null;
  } catch (error2) {
    const typedError = error2;
    if (errorCheck) {
      assertFalse(
        errorCheck(typedError),
        `Error matching the expected condition was thrown!: ${JSONParser.stringify(error2)}`
      );
    } else {
      assertFails(`Function threw an error: ${JSONParser.stringify(error2)}`);
    }
    return typedError;
  }
};
var assertRejects = async (promise, errorCheck) => {
  try {
    await promise;
    throw new AssertionError("Function didn't throw expected error");
  } catch (error2) {
    if (!errorCheck) return;
    if (errorCheck instanceof Error) assertDeepEqual(error2, errorCheck);
    else assertTrue(errorCheck(error2));
  }
};
var assertMatches = (actual, expected, message2) => {
  if (!isSubset(actual, expected))
    throw new AssertionError(
      _nullishCoalesce(message2, () => ( `subObj:
${JSONParser.stringify(expected)}
is not subset of
${JSONParser.stringify(actual)}`))
    );
};
var assertDeepEqual = (actual, expected, message2) => {
  if (!deepEquals(actual, expected))
    throw new AssertionError(
      _nullishCoalesce(message2, () => ( `subObj:
${JSONParser.stringify(expected)}
is not equal to
${JSONParser.stringify(actual)}`))
    );
};
var assertNotDeepEqual = (actual, expected, message2) => {
  if (deepEquals(actual, expected))
    throw new AssertionError(
      _nullishCoalesce(message2, () => ( `subObj:
${JSONParser.stringify(expected)}
is equals to
${JSONParser.stringify(actual)}`))
    );
};
var assertThat = (item) => {
  return {
    isEqualTo: (other) => assertTrue(deepEquals(item, other))
  };
};
var assertDefined = (value, message2) => {
  assertOk(value, message2 instanceof Error ? message2.message : message2);
};
function assertFalse(condition, message2) {
  if (condition !== false)
    throw new AssertionError(_nullishCoalesce(message2, () => ( `Condition is true`)));
}
function assertTrue(condition, message2) {
  if (condition !== true)
    throw new AssertionError(_nullishCoalesce(message2, () => ( `Condition is false`)));
}
function assertOk(obj, message2) {
  if (!obj) throw new AssertionError(_nullishCoalesce(message2, () => ( `Condition is not truthy`)));
}
function assertEqual(expected, actual, message2) {
  if (expected !== actual)
    throw new AssertionError(
      `${_nullishCoalesce(message2, () => ( "Objects are not equal"))}:
Expected: ${JSONParser.stringify(expected)}
Actual: ${JSONParser.stringify(actual)}`
    );
}
function assertNotEqual(obj, other, message2) {
  if (obj === other)
    throw new AssertionError(
      _nullishCoalesce(message2, () => ( `Objects are equal: ${JSONParser.stringify(obj)}`))
    );
}
function assertIsNotNull(result) {
  assertNotEqual(result, null);
  assertOk(result);
}
function assertIsNull(result) {
  assertEqual(result, null);
}
var argValue = (value) => (arg) => deepEquals(arg, value);
var argMatches = (matches) => (arg) => matches(arg);
function verifyThat(fn) {
  return {
    calledTimes: (times) => {
      assertEqual(_optionalChain([fn, 'access', _37 => _37.mock, 'optionalAccess', _38 => _38.calls, 'optionalAccess', _39 => _39.length]), times);
    },
    notCalled: () => {
      assertEqual(_optionalChain([fn, 'optionalAccess', _40 => _40.mock, 'optionalAccess', _41 => _41.calls, 'optionalAccess', _42 => _42.length]), 0);
    },
    called: () => {
      assertTrue(
        _optionalChain([fn, 'access', _43 => _43.mock, 'optionalAccess', _44 => _44.calls, 'access', _45 => _45.length]) !== void 0 && fn.mock.calls.length > 0
      );
    },
    calledWith: (...args) => {
      assertTrue(
        _optionalChain([fn, 'access', _46 => _46.mock, 'optionalAccess', _47 => _47.calls, 'access', _48 => _48.length]) !== void 0 && fn.mock.calls.length >= 1 && fn.mock.calls.some((call) => deepEquals(call.arguments, args))
      );
    },
    calledOnceWith: (...args) => {
      assertTrue(
        _optionalChain([fn, 'access', _49 => _49.mock, 'optionalAccess', _50 => _50.calls, 'access', _51 => _51.length]) !== void 0 && fn.mock.calls.length === 1 && fn.mock.calls.some((call) => deepEquals(call.arguments, args))
      );
    },
    calledWithArgumentMatching: (...matches) => {
      assertTrue(
        _optionalChain([fn, 'access', _52 => _52.mock, 'optionalAccess', _53 => _53.calls, 'access', _54 => _54.length]) !== void 0 && fn.mock.calls.length >= 1
      );
      assertTrue(
        _optionalChain([fn, 'access', _55 => _55.mock, 'optionalAccess', _56 => _56.calls, 'access', _57 => _57.length]) !== void 0 && fn.mock.calls.length >= 1 && fn.mock.calls.some(
          (call) => call.arguments && call.arguments.length >= matches.length && matches.every((match, index) => match(call.arguments[index]))
        )
      );
    },
    notCalledWithArgumentMatching: (...matches) => {
      assertFalse(
        _optionalChain([fn, 'access', _58 => _58.mock, 'optionalAccess', _59 => _59.calls, 'access', _60 => _60.length]) !== void 0 && fn.mock.calls.length >= 1 && fn.mock.calls[0].arguments && fn.mock.calls[0].arguments.length >= matches.length && matches.every(
          (match, index) => match(fn.mock.calls[0].arguments[index])
        )
      );
    }
  };
}
var assertThatArray = (array) => {
  return {
    isEmpty: () => assertEqual(
      array.length,
      0,
      `Array is not empty ${JSONParser.stringify(array)}`
    ),
    isNotEmpty: () => assertNotEqual(array.length, 0, `Array is empty`),
    hasSize: (length) => assertEqual(array.length, length),
    containsElements: (other) => {
      assertTrue(other.every((ts) => array.some((o) => deepEquals(ts, o))));
    },
    containsElementsMatching: (other) => {
      assertTrue(other.every((ts) => array.some((o) => isSubset(o, ts))));
    },
    containsOnlyElementsMatching: (other) => {
      assertEqual(array.length, other.length, `Arrays lengths don't match`);
      assertTrue(other.every((ts) => array.some((o) => isSubset(o, ts))));
    },
    containsExactlyInAnyOrder: (other) => {
      assertEqual(array.length, other.length);
      assertTrue(array.every((ts) => other.some((o) => deepEquals(ts, o))));
    },
    containsExactlyInAnyOrderElementsOf: (other) => {
      assertEqual(array.length, other.length);
      assertTrue(array.every((ts) => other.some((o) => deepEquals(ts, o))));
    },
    containsExactlyElementsOf: (other) => {
      assertEqual(array.length, other.length);
      for (let i = 0; i < array.length; i++) {
        assertTrue(deepEquals(array[i], other[i]));
      }
    },
    containsExactly: (elem) => {
      assertEqual(array.length, 1);
      assertTrue(deepEquals(array[0], elem));
    },
    contains: (elem) => {
      assertTrue(array.some((a) => deepEquals(a, elem)));
    },
    containsOnlyOnceElementsOf: (other) => {
      assertTrue(
        other.map((o) => array.filter((a) => deepEquals(a, o)).length).filter((a) => a === 1).length === other.length
      );
    },
    containsAnyOf: (other) => {
      assertTrue(array.some((a) => other.some((o) => deepEquals(a, o))));
    },
    allMatch: (matches) => {
      assertTrue(array.every(matches));
    },
    anyMatches: (matches) => {
      assertTrue(array.some(matches));
    },
    allMatchAsync: async (matches) => {
      for (const item of array) {
        assertTrue(await matches(item));
      }
    }
  };
};

// src/testing/deciderSpecification.ts
var DeciderSpecification = {
  for: deciderSpecificationFor
};
function deciderSpecificationFor(decider) {
  {
    return (givenEvents) => {
      return {
        when: (command2) => {
          const handle = () => {
            const existingEvents = Array.isArray(givenEvents) ? givenEvents : [givenEvents];
            const currentState = existingEvents.reduce(
              decider.evolve,
              decider.initialState()
            );
            return decider.decide(command2, currentState);
          };
          return {
            then: (expectedEvents) => {
              const resultEvents = handle();
              if (resultEvents instanceof Promise) {
                return resultEvents.then((events) => {
                  thenHandler(events, expectedEvents);
                });
              }
              thenHandler(resultEvents, expectedEvents);
            },
            thenNothingHappened: () => {
              const resultEvents = handle();
              if (resultEvents instanceof Promise) {
                return resultEvents.then((events) => {
                  thenNothingHappensHandler(events);
                });
              }
              thenNothingHappensHandler(resultEvents);
            },
            thenThrows: (...args) => {
              try {
                const result = handle();
                if (result instanceof Promise) {
                  return result.then(() => {
                    throw new AssertionError(
                      "Handler did not fail as expected"
                    );
                  }).catch((error2) => {
                    thenThrowsErrorHandler(error2, args);
                  });
                }
                throw new AssertionError("Handler did not fail as expected");
              } catch (error2) {
                thenThrowsErrorHandler(error2, args);
              }
            }
          };
        }
      };
    };
  }
}
function thenHandler(events, expectedEvents) {
  const resultEventsArray = Array.isArray(events) ? events : [events];
  const expectedEventsArray = Array.isArray(expectedEvents) ? expectedEvents : [expectedEvents];
  assertThatArray(resultEventsArray).containsOnlyElementsMatching(
    expectedEventsArray
  );
}
function thenNothingHappensHandler(events) {
  const resultEventsArray = Array.isArray(events) ? events : [events];
  assertThatArray(resultEventsArray).isEmpty();
}
function thenThrowsErrorHandler(error2, args) {
  if (error2 instanceof AssertionError) throw error2;
  if (args.length === 0) return;
  if (!_chunkWND32L6Pcjs.isErrorConstructor.call(void 0, args[0])) {
    assertTrue(
      args[0](error2),
      `Error didn't match the error condition: ${_optionalChain([error2, 'optionalAccess', _61 => _61.toString, 'call', _62 => _62()])}`
    );
    return;
  }
  assertTrue(
    error2 instanceof args[0],
    `Caught error is not an instance of the expected type: ${_optionalChain([error2, 'optionalAccess', _63 => _63.toString, 'call', _64 => _64()])}`
  );
  if (args[1]) {
    assertTrue(
      args[1](error2),
      `Error didn't match the error condition: ${_optionalChain([error2, 'optionalAccess', _65 => _65.toString, 'call', _66 => _66()])}`
    );
  }
}

// src/testing/wrapEventStore.ts
var WrapEventStore = (eventStore) => {
  const appendedEvents = /* @__PURE__ */ new Map();
  const wrapped = {
    ...eventStore,
    aggregateStream(streamName, options) {
      return eventStore.aggregateStream(streamName, options);
    },
    async readStream(streamName, options) {
      return await eventStore.readStream(
        streamName,
        options
      );
    },
    appendToStream: async (streamName, events, options) => {
      const result = await eventStore.appendToStream(
        streamName,
        events,
        options
      );
      const currentStream = _nullishCoalesce(appendedEvents.get(streamName), () => ( [streamName, []]));
      appendedEvents.set(streamName, [
        streamName,
        [...currentStream[1], ...events]
      ]);
      return result;
    },
    appendedEvents,
    setup: async (streamName, events) => {
      return eventStore.appendToStream(streamName, events);
    }
    // streamEvents: (): ReadableStream<
    //   // eslint-disable-next-line @typescript-eslint/no-redundant-type-constituents
    //   ReadEvent<Event, ReadEventMetadataType> | GlobalSubscriptionEvent
    // > => {
    //   return eventStore.streamEvents();
    // },
  };
  return wrapped;
};

// src/eventStore/projections/inMemory/inMemoryProjectionSpec.ts
var InMemoryProjectionSpec = {
  for: (options) => {
    const { projection: projection2 } = options;
    return (givenEvents) => {
      return {
        when: (events, options2) => {
          const allEvents = [];
          const run = async (database) => {
            let globalPosition = 0n;
            const numberOfTimes = _nullishCoalesce(_optionalChain([options2, 'optionalAccess', _67 => _67.numberOfTimes]), () => ( 1));
            for (const event2 of [
              ...givenEvents,
              ...Array.from({ length: numberOfTimes }).flatMap(() => events)
            ]) {
              const metadata = {
                globalPosition: ++globalPosition,
                streamPosition: globalPosition,
                streamName: _nullishCoalesce(_optionalChain([event2, 'access', _68 => _68.metadata, 'optionalAccess', _69 => _69.streamName]), () => ( `test-${_uuid.v4.call(void 0, )}`)),
                messageId: _uuid.v4.call(void 0, )
              };
              allEvents.push({
                ...event2,
                kind: "Event",
                metadata: {
                  ...metadata,
                  ..."metadata" in event2 ? _nullishCoalesce(event2.metadata, () => ( {})) : {}
                }
              });
            }
            const mockEventStore = {
              database,
              aggregateStream: async () => {
                return Promise.resolve({
                  state: {},
                  currentStreamVersion: 0n,
                  streamExists: false
                });
              },
              readStream: async () => {
                return Promise.resolve({
                  events: [],
                  currentStreamVersion: 0n,
                  streamExists: false
                });
              },
              appendToStream: async () => {
                return Promise.resolve({
                  nextExpectedStreamVersion: 0n,
                  createdNewStream: false
                });
              },
              streamExists: async () => {
                return Promise.resolve(false);
              }
            };
            await handleInMemoryProjections({
              events: allEvents,
              projections: [projection2],
              database,
              eventStore: mockEventStore
            });
          };
          return {
            then: async (assertFn, message2) => {
              const database = getInMemoryDatabase();
              await run(database);
              const succeeded = await assertFn({ database });
              if (succeeded !== void 0 && succeeded === false) {
                assertFails(
                  _nullishCoalesce(message2, () => ( "Projection specification didn't match the criteria"))
                );
              }
            },
            thenThrows: async (...args) => {
              const database = getInMemoryDatabase();
              try {
                await run(database);
                throw new AssertionError("Handler did not fail as expected");
              } catch (error2) {
                if (error2 instanceof AssertionError) throw error2;
                if (args.length === 0) return;
                if (!_chunkWND32L6Pcjs.isErrorConstructor.call(void 0, args[0])) {
                  assertTrue(
                    args[0](error2),
                    `Error didn't match the error condition: ${_optionalChain([error2, 'optionalAccess', _70 => _70.toString, 'call', _71 => _71()])}`
                  );
                  return;
                }
                assertTrue(
                  error2 instanceof args[0],
                  `Caught error is not an instance of the expected type: ${_optionalChain([error2, 'optionalAccess', _72 => _72.toString, 'call', _73 => _73()])}`
                );
                if (args[1]) {
                  assertTrue(
                    args[1](error2),
                    `Error didn't match the error condition: ${_optionalChain([error2, 'optionalAccess', _74 => _74.toString, 'call', _75 => _75()])}`
                  );
                }
              }
            }
          };
        }
      };
    };
  }
};
var eventInStream = (streamName, event2) => {
  return {
    ...event2,
    metadata: {
      ..._nullishCoalesce(event2.metadata, () => ( {})),
      streamName: _nullishCoalesce(_optionalChain([event2, 'access', _76 => _76.metadata, 'optionalAccess', _77 => _77.streamName]), () => ( streamName))
    }
  };
};
var eventsInStream = (streamName, events) => {
  return events.map((e) => eventInStream(streamName, e));
};
var newEventsInStream = eventsInStream;
function documentExists(expected, options) {
  return async ({ database }) => {
    const collection = database.collection(options.inCollection);
    const document = await collection.findOne((doc) => {
      const docId = "_id" in doc ? doc._id : void 0;
      return docId === options.withId;
    });
    if (!document) {
      assertFails(
        `Document with ID ${options.withId} does not exist in collection ${options.inCollection}`
      );
      return Promise.resolve(false);
    }
    for (const [key, value] of Object.entries(expected)) {
      const propKey = key;
      if (!(key in document) || JSONParser.stringify(document[propKey]) !== JSONParser.stringify(value)) {
        assertFails(`Property ${key} doesn't match the expected value`);
        return Promise.resolve(false);
      }
    }
    return Promise.resolve(true);
  };
}
var expectInMemoryDocuments = {
  fromCollection: (collectionName) => ({
    withId: (id) => ({
      toBeEqual: (expected) => documentExists(expected, {
        inCollection: collectionName,
        withId: id
      })
    })
  })
};

// src/eventStore/versioning/downcasting.ts
var downcastRecordedMessage = (recordedMessage, options) => {
  if (!_optionalChain([options, 'optionalAccess', _78 => _78.downcast]))
    return recordedMessage;
  const downcasted = options.downcast(
    recordedMessage
  );
  return {
    ...recordedMessage,
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    data: downcasted.data,
    ..."metadata" in recordedMessage || "metadata" in downcasted ? {
      metadata: {
        ..."metadata" in recordedMessage ? recordedMessage.metadata : {},
        ..."metadata" in downcasted ? downcasted.metadata : {}
      }
    } : {}
  };
};
var downcastRecordedMessages = (recordedMessages, options) => {
  if (!_optionalChain([options, 'optionalAccess', _79 => _79.downcast]))
    return recordedMessages;
  return recordedMessages.map(
    (recordedMessage) => downcastRecordedMessage(recordedMessage, options)
  );
};

// src/eventStore/versioning/upcasting.ts
var upcastRecordedMessage = (recordedMessage, options) => {
  if (!_optionalChain([options, 'optionalAccess', _80 => _80.upcast]))
    return recordedMessage;
  const upcasted = options.upcast(
    recordedMessage
  );
  return {
    ...recordedMessage,
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    data: upcasted.data,
    ..."metadata" in recordedMessage || "metadata" in upcasted ? {
      metadata: {
        ..."metadata" in recordedMessage ? recordedMessage.metadata : {},
        ..."metadata" in upcasted ? upcasted.metadata : {}
      }
    } : {}
  };
};
var upcastRecordedMessages = (recordedMessages, options) => {
  if (!_optionalChain([options, 'optionalAccess', _81 => _81.upcast]))
    return recordedMessages;
  return recordedMessages.map(
    (recordedMessage) => upcastRecordedMessage(recordedMessage, options)
  );
};

// src/eventStore/inMemoryEventStore.ts
var InMemoryEventStoreDefaultStreamVersion = 0n;
var getInMemoryEventStore = (eventStoreOptions) => {
  const streams = /* @__PURE__ */ new Map();
  const getAllEventsCount = () => {
    return Array.from(streams.values()).map((s) => s.length).reduce((p, c) => p + c, 0);
  };
  const database = _optionalChain([eventStoreOptions, 'optionalAccess', _82 => _82.database]) || getInMemoryDatabase();
  const inlineProjections2 = (_nullishCoalesce(_optionalChain([eventStoreOptions, 'optionalAccess', _83 => _83.projections]), () => ( []))).filter(({ type }) => type === "inline").map(({ projection: projection2 }) => projection2);
  const eventStore = {
    database,
    async aggregateStream(streamName, options) {
      const { evolve, initialState, read } = options;
      const result = await this.readStream(
        streamName,
        read
      );
      const events = _nullishCoalesce(_optionalChain([result, 'optionalAccess', _84 => _84.events]), () => ( []));
      const state = events.reduce((s, e) => evolve(s, e), initialState());
      return {
        currentStreamVersion: BigInt(events.length),
        state,
        streamExists: result.streamExists
      };
    },
    readStream: (streamName, options) => {
      const events = streams.get(streamName);
      const currentStreamVersion = events ? BigInt(events.length) : InMemoryEventStoreDefaultStreamVersion;
      assertExpectedVersionMatchesCurrent(
        currentStreamVersion,
        _optionalChain([options, 'optionalAccess', _85 => _85.expectedStreamVersion]),
        InMemoryEventStoreDefaultStreamVersion
      );
      const from = Number(_nullishCoalesce(_optionalChain([options, 'optionalAccess', _86 => _86.from]), () => ( 0)));
      const to = Number(
        _nullishCoalesce(_optionalChain([options, 'optionalAccess', _87 => _87.to]), () => ( (_optionalChain([options, 'optionalAccess', _88 => _88.maxCount]) ? (_nullishCoalesce(options.from, () => ( 0n))) + options.maxCount : _nullishCoalesce(_optionalChain([events, 'optionalAccess', _89 => _89.length]), () => ( 1)))))
      );
      const resultEvents = events !== void 0 && events.length > 0 ? upcastRecordedMessages(
        events.slice(from, to),
        _optionalChain([options, 'optionalAccess', _90 => _90.schema, 'optionalAccess', _91 => _91.versioning])
      ) : [];
      const result = {
        currentStreamVersion,
        events: resultEvents,
        streamExists: events !== void 0 && events.length > 0
      };
      return Promise.resolve(result);
    },
    appendToStream: async (streamName, events, options) => {
      const currentEvents = _nullishCoalesce(streams.get(streamName), () => ( []));
      const currentStreamVersion = currentEvents.length > 0 ? BigInt(currentEvents.length) : InMemoryEventStoreDefaultStreamVersion;
      assertExpectedVersionMatchesCurrent(
        currentStreamVersion,
        _optionalChain([options, 'optionalAccess', _92 => _92.expectedStreamVersion]),
        InMemoryEventStoreDefaultStreamVersion
      );
      const newEvents = events.map((event2, index) => {
        const metadata = {
          streamName,
          messageId: _uuid.v4.call(void 0, ),
          streamPosition: BigInt(currentEvents.length + index + 1),
          globalPosition: BigInt(getAllEventsCount() + index + 1)
        };
        return {
          ...event2,
          kind: _nullishCoalesce(event2.kind, () => ( "Event")),
          metadata: {
            ..."metadata" in event2 ? _nullishCoalesce(event2.metadata, () => ( {})) : {},
            ...metadata
          }
        };
      });
      const positionOfLastEventInTheStream = BigInt(
        newEvents.slice(-1)[0].metadata.streamPosition
      );
      streams.set(streamName, [
        ...currentEvents,
        ...downcastRecordedMessages(newEvents, _optionalChain([options, 'optionalAccess', _93 => _93.schema, 'optionalAccess', _94 => _94.versioning]))
      ]);
      if (inlineProjections2.length > 0) {
        await handleInMemoryProjections({
          projections: inlineProjections2,
          events: newEvents,
          database: eventStore.database,
          eventStore
        });
      }
      const result = {
        nextExpectedStreamVersion: positionOfLastEventInTheStream,
        createdNewStream: currentStreamVersion === InMemoryEventStoreDefaultStreamVersion
      };
      await tryPublishMessagesAfterCommit(
        newEvents,
        _optionalChain([eventStoreOptions, 'optionalAccess', _95 => _95.hooks])
      );
      return result;
    },
    streamExists: (streamName) => {
      const events = streams.get(streamName);
      return Promise.resolve(events !== void 0 && events.length > 0);
    }
  };
  return eventStore;
};

// src/commandHandling/handleCommand.ts
var CommandHandlerStreamVersionConflictRetryOptions = {
  retries: 3,
  minTimeout: 100,
  factor: 1.5,
  shouldRetryError: isExpectedVersionConflictError
};
var fromCommandHandlerRetryOptions = (retryOptions) => {
  if (retryOptions === void 0) return NoRetries;
  if ("onVersionConflict" in retryOptions) {
    if (typeof retryOptions.onVersionConflict === "boolean")
      return CommandHandlerStreamVersionConflictRetryOptions;
    else if (typeof retryOptions.onVersionConflict === "number")
      return {
        ...CommandHandlerStreamVersionConflictRetryOptions,
        retries: retryOptions.onVersionConflict
      };
    else return retryOptions.onVersionConflict;
  }
  return retryOptions;
};
var CommandHandler = (options) => async (store, id, handle, handleOptions) => asyncRetry(
  async () => {
    const result = await withSession(store, async ({ eventStore }) => {
      const { evolve, initialState } = options;
      const mapToStreamId = _nullishCoalesce(options.mapToStreamId, () => ( ((id2) => id2)));
      const streamName = mapToStreamId(id);
      const aggregationResult = await eventStore.aggregateStream(streamName, {
        evolve,
        initialState,
        read: {
          schema: options.schema,
          ...handleOptions,
          // expected stream version is passed to fail fast
          // if stream is in the wrong state
          // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
          expectedStreamVersion: _nullishCoalesce(_optionalChain([handleOptions, 'optionalAccess', _96 => _96.expectedStreamVersion]), () => ( NO_CONCURRENCY_CHECK))
        }
      });
      const {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        currentStreamVersion,
        streamExists: _streamExists,
        ...restOfAggregationResult
      } = aggregationResult;
      let state = aggregationResult.state;
      const handlers = Array.isArray(handle) ? handle : [handle];
      let eventsToAppend = [];
      for (const handler of handlers) {
        const result2 = await handler(state);
        const newEvents = Array.isArray(result2) ? result2 : [result2];
        if (newEvents.length > 0) {
          state = newEvents.reduce(evolve, state);
        }
        eventsToAppend = [...eventsToAppend, ...newEvents];
      }
      if (eventsToAppend.length === 0) {
        return {
          ...restOfAggregationResult,
          newEvents: [],
          newState: state,
          // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
          nextExpectedStreamVersion: currentStreamVersion,
          createdNewStream: false
        };
      }
      const expectedStreamVersion = _nullishCoalesce(_optionalChain([handleOptions, 'optionalAccess', _97 => _97.expectedStreamVersion]), () => ( (aggregationResult.streamExists ? currentStreamVersion : STREAM_DOES_NOT_EXIST)));
      const appendResult = await eventStore.appendToStream(
        streamName,
        eventsToAppend,
        {
          ...handleOptions,
          expectedStreamVersion
        }
      );
      return {
        ...appendResult,
        newEvents: eventsToAppend,
        newState: state
      };
    });
    return result;
  },
  fromCommandHandlerRetryOptions(
    handleOptions && "retry" in handleOptions ? handleOptions.retry : options.retry
  )
);
var withSession = (eventStore, callback) => {
  const sessionFactory = canCreateEventStoreSession(eventStore) ? eventStore : nulloSessionFactory(eventStore);
  return sessionFactory.withSession(callback);
};

// src/commandHandling/handleCommandWithDecider.ts
var DeciderCommandHandler = (options) => async (eventStore, id, commands, handleOptions) => {
  const { decide, ...rest } = options;
  const deciders = (Array.isArray(commands) ? commands : [commands]).map(
    (command2) => (state) => decide(command2, state)
  );
  return CommandHandler(rest)(
    eventStore,
    id,
    deciders,
    handleOptions
  );
};

// src/messageBus/index.ts
var getInMemoryMessageBus = () => {
  const allHandlers = /* @__PURE__ */ new Map();
  let pendingMessages = [];
  return {
    send: async (command2) => {
      const handlers = allHandlers.get(command2.type);
      if (handlers === void 0 || handlers.length === 0)
        throw new (0, _chunkWND32L6Pcjs.EmmettError)(
          `No handler registered for command ${command2.type}!`
        );
      const commandHandler = handlers[0];
      await commandHandler(command2);
    },
    publish: async (event2) => {
      const handlers = _nullishCoalesce(allHandlers.get(event2.type), () => ( []));
      for (const handler of handlers) {
        const eventHandler = handler;
        await eventHandler(event2);
      }
    },
    schedule: (message2, when) => {
      pendingMessages = [...pendingMessages, { message: message2, options: when }];
    },
    handle: (commandHandler, ...commandTypes) => {
      const alreadyRegistered = [...allHandlers.keys()].filter(
        (registered) => commandTypes.includes(registered)
      );
      if (alreadyRegistered.length > 0)
        throw new (0, _chunkWND32L6Pcjs.EmmettError)(
          `Cannot register handler for commands ${alreadyRegistered.join(", ")} as they're already registered!`
        );
      for (const commandType of commandTypes) {
        allHandlers.set(commandType, [
          commandHandler
        ]);
      }
    },
    subscribe(eventHandler, ...eventTypes) {
      for (const eventType of eventTypes) {
        if (!allHandlers.has(eventType)) allHandlers.set(eventType, []);
        allHandlers.set(eventType, [
          ..._nullishCoalesce(allHandlers.get(eventType), () => ( [])),
          eventHandler
        ]);
      }
    },
    dequeue: () => {
      const pending = pendingMessages;
      pendingMessages = [];
      return pending;
    }
  };
};

// src/processors/processors.ts

var getCheckpoint = (message2) => {
  return "checkpoint" in message2.metadata ? (
    // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
    message2.metadata.checkpoint
  ) : "globalPosition" in message2.metadata && // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
  _chunkWND32L6Pcjs.isBigint.call(void 0, message2.metadata.globalPosition) ? (
    // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
    message2.metadata.globalPosition
  ) : "streamPosition" in message2.metadata && // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
  _chunkWND32L6Pcjs.isBigint.call(void 0, message2.metadata.streamPosition) ? (
    // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
    message2.metadata.streamPosition
  ) : null;
};
var wasMessageHandled = (message2, checkpoint) => {
  const messageCheckpoint = getCheckpoint(message2);
  const checkpointBigint = checkpoint;
  return messageCheckpoint !== null && messageCheckpoint !== void 0 && checkpointBigint !== null && checkpointBigint !== void 0 && messageCheckpoint <= checkpointBigint;
};
var MessageProcessorType = {
  PROJECTOR: "projector",
  REACTOR: "reactor"
};
var MessageProcessor = {
  result: {
    skip: (options) => ({
      type: "SKIP",
      ..._nullishCoalesce(options, () => ( {}))
    }),
    stop: (options) => ({
      type: "STOP",
      ..._nullishCoalesce(options, () => ( {}))
    })
  }
};
var defaultProcessingMessageProcessingScope = (handler, partialContext) => handler(partialContext);
var defaultProcessorVersion = 1;
var defaultProcessorPartition = defaultTag;
var getProcessorInstanceId = (processorId) => `${processorId}:${_uuid.v7.call(void 0, )}`;
var getProjectorId = (options) => `emt:processor:projector:${options.projectionName}`;
var reactor = (options) => {
  const {
    checkpoints,
    processorId,
    processorInstanceId: instanceId = getProcessorInstanceId(processorId),
    type = MessageProcessorType.REACTOR,
    version = defaultProcessorVersion,
    partition = defaultProcessorPartition,
    hooks = {},
    processingScope = defaultProcessingMessageProcessingScope,
    startFrom,
    canHandle,
    stopAfter
  } = options;
  const eachMessage = "eachMessage" in options && options.eachMessage ? options.eachMessage : () => Promise.resolve();
  let isInitiated = false;
  let isActive = false;
  let lastCheckpoint = null;
  let closeSignal = null;
  const init = async (initOptions) => {
    if (isInitiated) return;
    if (hooks.onInit === void 0) {
      isInitiated = true;
      return;
    }
    return await processingScope(async (context) => {
      await hooks.onInit(context);
      isInitiated = true;
    }, initOptions);
  };
  const close = async (closeOptions) => {
    isActive = false;
    if (closeSignal) {
      closeSignal();
      closeSignal = null;
    }
    if (hooks.onClose) {
      await processingScope(hooks.onClose, closeOptions);
    }
  };
  return {
    // TODO: Consider whether not make it optional or add URN prefix
    id: processorId,
    instanceId,
    type,
    init,
    start: async (startOptions) => {
      if (isActive) {
        console.log(
          `Processor ${processorId} with instance id ${instanceId} is already active. Start request ignored.`
        );
        return;
      }
      console.log(
        `Starting processor ${processorId} with instance id ${instanceId}`
      );
      await init(startOptions);
      isActive = true;
      closeSignal = onShutdown(() => close({}));
      if (lastCheckpoint !== null) {
        console.log(
          `Processor ${processorId} started with instance id ${instanceId}, checkpoint: ${JSONParser.stringify(lastCheckpoint)}`
        );
        return {
          lastCheckpoint
        };
      }
      return await processingScope(async (context) => {
        if (hooks.onStart) {
          console.log(
            `Executing onStart hook for processor ${processorId} with instance id ${instanceId}`
          );
          await hooks.onStart(context);
        }
        if (startFrom && startFrom !== "CURRENT") {
          console.log(
            `Processor ${processorId} with instance id ${instanceId} starting from: ${JSONParser.stringify(startFrom)}`
          );
          return startFrom;
        }
        if (checkpoints) {
          const readResult = await _optionalChain([checkpoints, 'optionalAccess', _98 => _98.read, 'call', _99 => _99(
            {
              processorId,
              partition
            },
            { ...startOptions, ...context }
          )]);
          lastCheckpoint = readResult.lastCheckpoint;
        }
        if (lastCheckpoint === null) {
          console.log(
            `Processor ${processorId} with instance id ${instanceId} starting from: BEGINNING`
          );
          return "BEGINNING";
        }
        console.log(
          `Checkpoint read for processor ${processorId} with instance id ${instanceId}: ${JSONParser.stringify(lastCheckpoint)}`
        );
        return {
          lastCheckpoint
        };
      }, startOptions);
    },
    close,
    get isActive() {
      return isActive;
    },
    handle: async (messages, partialContext) => {
      if (!isActive) return Promise.resolve();
      try {
        return await processingScope(async (context) => {
          let result = void 0;
          for (const message2 of messages) {
            if (wasMessageHandled(message2, lastCheckpoint)) continue;
            const upcasted = upcastRecordedMessage(
              // TODO: Make it smarter
              message2,
              _optionalChain([options, 'access', _100 => _100.messageOptions, 'optionalAccess', _101 => _101.schema, 'optionalAccess', _102 => _102.versioning])
            );
            if (canHandle !== void 0 && !canHandle.includes(upcasted.type))
              continue;
            const messageProcessingResult = await eachMessage(
              upcasted,
              context
            );
            if (checkpoints) {
              const storeCheckpointResult = await checkpoints.store(
                {
                  processorId,
                  version,
                  message: upcasted,
                  lastCheckpoint,
                  partition
                },
                context
              );
              if (storeCheckpointResult.success) {
                lastCheckpoint = storeCheckpointResult.newCheckpoint;
              }
            }
            if (messageProcessingResult && messageProcessingResult.type === "STOP") {
              isActive = false;
              result = messageProcessingResult;
              break;
            }
            if (stopAfter && stopAfter(upcasted)) {
              isActive = false;
              result = { type: "STOP", reason: "Stop condition reached" };
              break;
            }
            if (messageProcessingResult && messageProcessingResult.type === "SKIP")
              continue;
          }
          return result;
        }, partialContext);
      } catch (error2) {
        console.log(
          `Error during message processing for processor ${processorId} with instance id ${instanceId}. Stopping the processor.`,
          error2
        );
        isActive = false;
        return {
          type: "STOP",
          error: error2,
          reason: "Error during message processing"
        };
      }
    }
  };
};
var projector = (options) => {
  const {
    projection: projection2,
    processorId = getProjectorId({
      projectionName: _nullishCoalesce(projection2.name, () => ( "unknown"))
    }),
    ...rest
  } = options;
  return reactor({
    ...rest,
    type: MessageProcessorType.PROJECTOR,
    canHandle: projection2.canHandle,
    processorId,
    messageOptions: options.projection.eventsOptions,
    hooks: {
      onInit: _optionalChain([options, 'access', _103 => _103.hooks, 'optionalAccess', _104 => _104.onInit]),
      onStart: options.truncateOnStart && options.projection.truncate || _optionalChain([options, 'access', _105 => _105.hooks, 'optionalAccess', _106 => _106.onStart]) ? async (context) => {
        if (options.truncateOnStart && options.projection.truncate)
          await options.projection.truncate(context);
        if (_optionalChain([options, 'access', _107 => _107.hooks, 'optionalAccess', _108 => _108.onStart])) await _optionalChain([options, 'access', _109 => _109.hooks, 'optionalAccess', _110 => _110.onStart, 'call', _111 => _111(context)]);
      } : void 0,
      onClose: _optionalChain([options, 'access', _112 => _112.hooks, 'optionalAccess', _113 => _113.onClose])
    },
    eachMessage: async (event2, context) => projection2.handle([event2], context)
  });
};

// src/processors/inMemoryProcessors.ts
var inMemoryCheckpointer = () => {
  return {
    read: async ({ processorId }, { database }) => {
      const checkpoint = await database.collection("emt_processor_checkpoints").findOne((d) => d._id === processorId);
      return Promise.resolve({
        lastCheckpoint: _nullishCoalesce(_optionalChain([checkpoint, 'optionalAccess', _114 => _114.lastCheckpoint]), () => ( null))
      });
    },
    store: async (context, { database }) => {
      const { message: message2, processorId, lastCheckpoint } = context;
      const checkpoints = database.collection(
        "emt_processor_checkpoints"
      );
      const checkpoint = await checkpoints.findOne(
        (d) => d._id === processorId
      );
      const currentPosition = _nullishCoalesce(_optionalChain([checkpoint, 'optionalAccess', _115 => _115.lastCheckpoint]), () => ( null));
      const newCheckpoint = getCheckpoint(message2);
      if (currentPosition && (currentPosition === newCheckpoint || currentPosition !== lastCheckpoint)) {
        return {
          success: false,
          reason: currentPosition === newCheckpoint ? "IGNORED" : newCheckpoint !== null && currentPosition > newCheckpoint ? "CURRENT_AHEAD" : "MISMATCH"
        };
      }
      await checkpoints.handle(processorId, (existing) => ({
        ..._nullishCoalesce(existing, () => ( {})),
        _id: processorId,
        lastCheckpoint: newCheckpoint
      }));
      return { success: true, newCheckpoint };
    }
  };
};
var inMemoryProcessingScope = (options) => {
  const processorDatabase = options.database;
  const processingScope = (handler, partialContext) => {
    const database = _nullishCoalesce(processorDatabase, () => ( _optionalChain([partialContext, 'optionalAccess', _116 => _116.database])));
    if (!database)
      throw new (0, _chunkWND32L6Pcjs.EmmettError)(
        `InMemory processor '${options.processorId}' is missing database. Ensure that you passed it through options`
      );
    return handler({ ...partialContext, database });
  };
  return processingScope;
};
var inMemoryProjector = (options) => {
  const database = _nullishCoalesce(_optionalChain([options, 'access', _117 => _117.connectionOptions, 'optionalAccess', _118 => _118.database]), () => ( getInMemoryDatabase()));
  const hooks = {
    onInit: _optionalChain([options, 'access', _119 => _119.hooks, 'optionalAccess', _120 => _120.onInit]),
    onStart: _optionalChain([options, 'access', _121 => _121.hooks, 'optionalAccess', _122 => _122.onStart]),
    onClose: _optionalChain([options, 'access', _123 => _123.hooks, 'optionalAccess', _124 => _124.onClose]) ? async (context) => {
      if (_optionalChain([options, 'access', _125 => _125.hooks, 'optionalAccess', _126 => _126.onClose])) await _optionalChain([options, 'access', _127 => _127.hooks, 'optionalAccess', _128 => _128.onClose, 'call', _129 => _129(context)]);
    } : void 0
  };
  const processor = projector({
    ...options,
    hooks,
    processingScope: inMemoryProcessingScope({
      database,
      processorId: _nullishCoalesce(options.processorId, () => ( `projection:${options.projection.name}`))
    }),
    checkpoints: inMemoryCheckpointer()
  });
  return Object.assign(processor, { database });
};
var inMemoryReactor = (options) => {
  const database = _nullishCoalesce(_optionalChain([options, 'access', _130 => _130.connectionOptions, 'optionalAccess', _131 => _131.database]), () => ( getInMemoryDatabase()));
  const hooks = {
    onInit: _optionalChain([options, 'access', _132 => _132.hooks, 'optionalAccess', _133 => _133.onInit]),
    onStart: _optionalChain([options, 'access', _134 => _134.hooks, 'optionalAccess', _135 => _135.onStart]),
    onClose: _optionalChain([options, 'access', _136 => _136.hooks, 'optionalAccess', _137 => _137.onClose])
  };
  const processor = reactor({
    ...options,
    hooks,
    processingScope: inMemoryProcessingScope({
      database,
      processorId: options.processorId
    }),
    checkpoints: inMemoryCheckpointer()
  });
  return Object.assign(processor, { database });
};

// src/projections/index.ts
var filterProjections = (type, projections2) => {
  const inlineProjections2 = projections2.filter((projection2) => projection2.type === type).map(({ projection: projection2 }) => projection2);
  const duplicateRegistrations = arrayUtils.getDuplicates(
    inlineProjections2,
    (proj) => proj.name
  );
  if (duplicateRegistrations.length > 0) {
    throw new (0, _chunkWND32L6Pcjs.EmmettError)(`You cannot register multiple projections with the same name (or without the name).
      Ensure that:
      ${JSONParser.stringify(duplicateRegistrations)}
      have different names`);
  }
  return inlineProjections2;
};
var projection = (definition) => definition;
var inlineProjections = (definitions) => definitions.map((definition) => ({
  type: "inline",
  projection: definition
}));
var asyncProjections = (definitions) => definitions.map((definition) => ({
  type: "inline",
  projection: definition
}));
var projections = {
  inline: inlineProjections,
  async: asyncProjections
};






































































































































exports.AssertionError = AssertionError; exports.CommandHandler = CommandHandler; exports.CommandHandlerStreamVersionConflictRetryOptions = CommandHandlerStreamVersionConflictRetryOptions; exports.ConcurrencyError = _chunkWND32L6Pcjs.ConcurrencyError; exports.ConcurrencyInMemoryDatabaseError = _chunkWND32L6Pcjs.ConcurrencyInMemoryDatabaseError; exports.DATABASE_REQUIRED_ERROR_MESSAGE = DATABASE_REQUIRED_ERROR_MESSAGE; exports.DeciderCommandHandler = DeciderCommandHandler; exports.DeciderSpecification = DeciderSpecification; exports.EmmettError = _chunkWND32L6Pcjs.EmmettError; exports.ExpectedVersionConflictError = ExpectedVersionConflictError; exports.GlobalStreamCaughtUpType = GlobalStreamCaughtUpType; exports.IllegalStateError = _chunkWND32L6Pcjs.IllegalStateError; exports.InMemoryEventStoreDefaultStreamVersion = InMemoryEventStoreDefaultStreamVersion; exports.InMemoryProjectionSpec = InMemoryProjectionSpec; exports.InProcessLock = InProcessLock; exports.JSONParser = JSONParser; exports.MessageProcessor = MessageProcessor; exports.MessageProcessorType = MessageProcessorType; exports.NO_CONCURRENCY_CHECK = NO_CONCURRENCY_CHECK; exports.NoRetries = NoRetries; exports.NotFoundError = _chunkWND32L6Pcjs.NotFoundError; exports.ParseError = ParseError; exports.STREAM_DOES_NOT_EXIST = STREAM_DOES_NOT_EXIST; exports.STREAM_EXISTS = STREAM_EXISTS; exports.TaskProcessor = TaskProcessor; exports.ValidationError = _chunkWND32L6Pcjs.ValidationError; exports.ValidationErrors = _chunkWND32L6Pcjs.ValidationErrors; exports.WrapEventStore = WrapEventStore; exports.accept = accept; exports.argMatches = argMatches; exports.argValue = argValue; exports.arrayUtils = arrayUtils; exports.assertDeepEqual = assertDeepEqual; exports.assertDefined = assertDefined; exports.assertDoesNotThrow = assertDoesNotThrow; exports.assertEqual = assertEqual; exports.assertExpectedVersionMatchesCurrent = assertExpectedVersionMatchesCurrent; exports.assertFails = assertFails; exports.assertFalse = assertFalse; exports.assertIsNotNull = assertIsNotNull; exports.assertIsNull = assertIsNull; exports.assertMatches = assertMatches; exports.assertNotDeepEqual = assertNotDeepEqual; exports.assertNotEmptyString = _chunkWND32L6Pcjs.assertNotEmptyString; exports.assertNotEqual = assertNotEqual; exports.assertOk = assertOk; exports.assertPositiveNumber = _chunkWND32L6Pcjs.assertPositiveNumber; exports.assertRejects = assertRejects; exports.assertThat = assertThat; exports.assertThatArray = assertThatArray; exports.assertThrows = assertThrows; exports.assertThrowsAsync = assertThrowsAsync; exports.assertTrue = assertTrue; exports.assertUnsignedBigInt = _chunkWND32L6Pcjs.assertUnsignedBigInt; exports.asyncAwaiter = asyncAwaiter; exports.asyncProjections = asyncProjections; exports.asyncRetry = asyncRetry; exports.bigInt = bigInt; exports.canCreateEventStoreSession = canCreateEventStoreSession; exports.caughtUpEventFrom = caughtUpEventFrom; exports.command = command; exports.complete = complete; exports.deepEquals = deepEquals; exports.defaultProcessingMessageProcessingScope = defaultProcessingMessageProcessingScope; exports.defaultProcessorPartition = defaultProcessorPartition; exports.defaultProcessorVersion = defaultProcessorVersion; exports.defaultTag = defaultTag; exports.delay = delay; exports.documentExists = documentExists; exports.downcastRecordedMessage = downcastRecordedMessage; exports.downcastRecordedMessages = downcastRecordedMessages; exports.emmettPrefix = emmettPrefix; exports.error = error; exports.event = event; exports.eventInStream = eventInStream; exports.eventsInStream = eventsInStream; exports.expectInMemoryDocuments = expectInMemoryDocuments; exports.filterProjections = filterProjections; exports.formatDateToUtcYYYYMMDD = _chunkWND32L6Pcjs.formatDateToUtcYYYYMMDD; exports.forwardToMessageBus = forwardToMessageBus; exports.getCheckpoint = getCheckpoint; exports.getInMemoryDatabase = getInMemoryDatabase; exports.getInMemoryEventStore = getInMemoryEventStore; exports.getInMemoryMessageBus = getInMemoryMessageBus; exports.getProcessorInstanceId = getProcessorInstanceId; exports.getProjectorId = getProjectorId; exports.globalStreamCaughtUp = globalStreamCaughtUp; exports.globalTag = globalTag; exports.handleInMemoryProjections = handleInMemoryProjections; exports.hashText = hashText; exports.ignore = ignore; exports.inMemoryCheckpointer = inMemoryCheckpointer; exports.inMemoryMultiStreamProjection = inMemoryMultiStreamProjection; exports.inMemoryProjection = inMemoryProjection; exports.inMemoryProjector = inMemoryProjector; exports.inMemoryReactor = inMemoryReactor; exports.inMemorySingleStreamProjection = inMemorySingleStreamProjection; exports.inlineProjections = inlineProjections; exports.isBigint = _chunkWND32L6Pcjs.isBigint; exports.isEquatable = isEquatable; exports.isErrorConstructor = _chunkWND32L6Pcjs.isErrorConstructor; exports.isExpectedVersionConflictError = isExpectedVersionConflictError; exports.isGlobalStreamCaughtUp = isGlobalStreamCaughtUp; exports.isNotInternalEvent = isNotInternalEvent; exports.isNumber = _chunkWND32L6Pcjs.isNumber; exports.isPluginConfig = _chunkWND32L6Pcjs.isPluginConfig; exports.isString = _chunkWND32L6Pcjs.isString; exports.isSubscriptionEvent = isSubscriptionEvent; exports.isSubset = isSubset; exports.isValidYYYYMMDD = _chunkWND32L6Pcjs.isValidYYYYMMDD; exports.matchesExpectedVersion = matchesExpectedVersion; exports.merge = merge; exports.message = message; exports.newEventsInStream = newEventsInStream; exports.nulloSessionFactory = nulloSessionFactory; exports.onShutdown = onShutdown; exports.parseDateFromUtcYYYYMMDD = _chunkWND32L6Pcjs.parseDateFromUtcYYYYMMDD; exports.projection = projection; exports.projections = projections; exports.projector = projector; exports.publish = publish; exports.reactor = reactor; exports.reply = reply; exports.schedule = schedule; exports.send = send; exports.sum = sum; exports.toNormalizedString = toNormalizedString; exports.tryPublishMessagesAfterCommit = tryPublishMessagesAfterCommit; exports.unknownTag = unknownTag; exports.upcastRecordedMessage = upcastRecordedMessage; exports.upcastRecordedMessages = upcastRecordedMessages; exports.verifyThat = verifyThat; exports.wasMessageHandled = wasMessageHandled;
//# sourceMappingURL=index.cjs.map