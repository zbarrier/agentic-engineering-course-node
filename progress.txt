## Codebase Patterns
- Routes in this project use raw `knex` queries directly against Postgres (see `ActiveReservations/routes.ts`, `PlaceReservation/routes.ts`) — not the `supabase`/`readmodel` helper shown in the generic `build-state-view` SKILL.md template. Follow the existing routes.ts pattern in the slice's own context folder over the skill's generic example when they differ.
- Migrations live at repo root `migrations/V{n}__*.sql` (not `supabase/migrations/` as the skill template says). Append-only — never edit an existing migration, add a new `V{n}` file.
- `.build-kit/.eventmodelers/config.json` is absent in this repo (only `install-manifest.json` exists there) — board sync / `update-slice-status` / MCP calls are skipped for this project; slice status is tracked locally in `.build-kit/.slices/<context>/index.json` only.
- `npm run run:checks -- --staged` mirrors the pre-commit hook exactly (staged-only); plain `npm run run:checks` checks ALL uncommitted changes including unrelated pre-existing working-tree modifications — use `--staged` after `git add`ing only the intended slice files to get an accurate pass/fail signal.
- slice.json readmodel/screen `fields[]` can rename a value relative to the underlying event's own field name (e.g. event field `NumberOfPeople` exposed as read-model/screen field `PartySize`) — the DB column and API/JSON field follow the slice.json name, but the event's TS type field itself is untouched unless the event-producing slice (out of scope here) is itself Planned.

## [2026-09-15] - Active Reservations

- Slice was already implemented (projection, routes, tests) from a prior iteration but re-appeared as "Planned" because slice.json's readmodel (`Active Reservations`) and screen (`My Reservations`) field definitions were renamed from `NumberOfPeople` to `PartySize`, while the underlying `ReservationPlaced`/`ReservationCancelled` event types (in `ReservationsEvents.ts`) still use `NumberOfPeople` — a legitimate rename that only affects this slice's own read model, not the shared event shape.
- Files changed: `migrations/V3__active_reservations_party_size.sql` (new, renames `number_of_people` -> `party_size` column), `src/slices/reservations/ActiveReservations/ActiveReservationsProjection.ts`, `src/slices/reservations/ActiveReservations/routes.ts`, `src/slices/reservations/ActiveReservations/ActiveReservations.test.ts`.
- Did not touch the "linked copy" cancellation note in slice.json's notes (`Cancellation is handled by the linked copy in column G`) — that's a board-diagram annotation about which column draws the inbound edge, not an instruction to remove the existing `ReservationCancelled` handling from this projection; removing it would regress working delete-on-cancel behavior with nothing yet in place to replace it (Cancel Reservation slice is still status "Created", not Planned).
- **Learnings for future iterations:**
  - When a "Planned" slice already has full-looking code, diff every `fields[]` entry in slice.json's `readmodels`/`screens`/`commands` against the actual TS types/column names before assuming nothing changed — a rename-only spec change is easy to miss since build/tests still pass against stale names until you look at slice.json itself.
  - Watch for internally inconsistent field-name examples inside a single slice.json's `storylines[]` (here, 4 of 5 example occurrences said `PartySize`, 1 said `NumberOfPeople`) — the canonical source is the node's own top-level `fields[]` array (in `readmodels`/`screens`/`commands`), not the storyline walkthrough examples, which can lag behind a rename.
  - This project has no `.githooks` installed (no `core.hooksPath` set, `.githooks/` doesn't exist) — the commit-scope guard only runs when explicitly invoked via `npm run run:checks`; run it manually with `-- --staged` before every slice commit since there's no hook to catch it automatically.
  - Pre-existing unrelated uncommitted changes were present in the working tree at the start of this iteration (OpenAPI doc comments added to `PlaceReservation/routes.ts` and `ActiveReservations/routes.ts`, plus new synced slice folders from a prior `load-slice` run) — left untouched and unstaged; only this slice's own files were staged and committed.
---
